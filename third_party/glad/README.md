# GLAD 2 — OpenGL 3.3 core

Generated loader used by the viewport (`gl.c`, `include/glad/gl.h`, `include/KHR/khrplatform.h`).

These three files are part of the Axiom tree. CMake will not fetch them; a clone without `src/gl.c` will fail configure on purpose so a missing upload is obvious.
